<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface"
    tools:context=".CalendarActivity">

    <ScrollView
        android:id="@+id/main"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp"
        android:clipToPadding="false"
        android:scrollbars="none"
        android:layout_marginBottom="80dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="center_horizontal">

            <com.google.android.material.textview.MaterialTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/welcome"
                android:textAppearance="?attr/textAppearanceTitleLarge"
                android:layout_marginTop="48dp"
                android:fontFamily="@font/rhodiumlibre_regular"/>

            <com.google.android.material.textview.MaterialTextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/username"
                android:textAppearance="?attr/textAppearanceTitleLarge"
                android:layout_marginTop="4dp"
                android:fontFamily="@font/rocksalt_regular" />

            <com.google.android.material.card.MaterialCardView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:cardCornerRadius="24dp"
                app:cardElevation="4dp"
                app:strokeColor="?attr/colorOutline"
                app:strokeWidth="1dp"
                android:layout_marginTop="48dp"
                android:layout_marginHorizontal="32dp"
                android:padding="8dp">

                <CalendarView
                    android:id="@+id/booking_calendar"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_margin="8dp"
                    android:showWeekNumber="false"
                    android:focusedMonthDateColor="?attr/colorPrimary"
                    android:selectedWeekBackgroundColor="?attr/colorPrimaryContainer"
                    android:weekSeparatorLineColor="?attr/colorOutline"
                    android:unfocusedMonthDateColor="?attr/colorOnSurfaceVariant"
                    android:weekDayTextAppearance="@style/TextAppearance.Material3.BodyMedium"
                    android:dateTextAppearance="@style/TextAppearance.Material3.BodyMedium"/>
            </com.google.android.material.card.MaterialCardView>
        </LinearLayout>
    </ScrollView>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="?attr/colorSurfaceContainer"
        app:menu="@menu/bottom_navigation_menu"
        app:labelVisibilityMode="labeled"
        app:elevation="8dp"
        app:itemIconTint="@color/bottom_nav_icon_color"
        app:itemTextColor="@color/bottom_nav_text_color" />
</FrameLayout>
